<%= form_for @appearance do |f| %>
  <%= f.collection_select(:guest, Guest.all, :id, :name, prompt: true) %>
  <%= f.collection_select(:episode, Episode.all, :id, :number, prompt: true) %>
  <%= f.label :rating %>
  <%= f.text_field :rating %>
  <%= f.submit %>
<% end %>
